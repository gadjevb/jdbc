CREATE TABLE PEOPLE(Name VARCHAR(50) NOT NULL, EGN BIGINT PRIMARY KEY NOT NULL, Age INT NOT NULL, Email VARCHAR(100) NOT NULL);
CREATE TABLE TRIP(EGN BIGINT REFERENCES PEOPLE(EGN) NOT NULL, Arrival DATE NOT NULL, Department DATE NOT NULL, City VARCHAR(100) NOT NULL);